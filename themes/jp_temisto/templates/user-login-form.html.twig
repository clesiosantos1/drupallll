<section id="user-login">
  <div class="container">
    <div class="row">
        <div class="col-12 col-xs-12 yg-creative-login">
            <div class="login-text">
              {#   <h2 class="title" style="text-align:center;"><strong>USUÁRIO <span>AUTENTICAÇÃO</span></strong></h2> #}
                <form role="form" id="sub-form">
                    <div class="form-group">
                     {{ form.name }}
                    </div>
                    <div class="form-group">
                    {{ form.pass }}
                    </div>
                   {#  <div class="form-group">
                        <a href="password" alt="Request new password via email">Esqueci a palavra-passe?</a>
                    </div> #}
                    {{ form.form_build_id }}
                    {{ form.form_id }}
                    <div class="form-group">
                  {#   <div class="login-button"> #}
                        {# <a href="register">Sign Up</a> #}
                        {{ form.actions }}
                    </div>
                </form>
            </div>
        </div>
    </div>
  </div>
</section>

<style>
  h2 {
    font-size: 40px;
    font-weight: 600;
    padding: 0 0 20px;
    color: #5a5a5a;
    font-family: 'Poppins', sans-serif;
    width: 100%;
    margin: auto;
}
  #user-login {
    max-width: 100% !important;
    border: none;
    padding: 0px 40px 40px;
    margin-top: 40px !important;
}

.login-text .form-group input {
    height: 50px;
}

select.form-control::placeholder, input[type="text"]::placeholder {
  color: #b4b4b4 !important;
}
.forgot-password {
    margin-top: -21px;
    font-size: 18px;
}

#user-login input[type=submit] {
    font-size: 18px;
    font-weight: 600;
    padding: 8px;
    width: 388px;
    border-radius: 4px;
    height: 50px;
    background-color: #15154687 !important;
}
#user-login input[type=submit]:hover {
    color: #009acb !important;
    background: #fff !important;
    border: 1px solid #009acb;
  }
  @media (min-width: 0px) and (max-width: 735.99px) {
    #user-login input[type=submit] {
    font-size: 13px;
    font-weight: 600;
    padding: 8px;
    width: 100% !important;
    border-radius: 4px;
    height: 50px;
    background-color: #15154687 !important;
    }
    .simplelogin-form .forgot-password {
      font-size: 13px !important;
    }
  }



</style>
